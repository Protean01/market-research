# Image Choice Question Type — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a new `image_mcq` question type where admins upload images per option in the builder and respondents pick one from a 2-column image grid that auto-advances the survey.

**Architecture:** New question type stored as `{label, image_url}[]` in the existing JSON `questions` column — no migration needed. A dedicated upload controller stores images to the `public` disk and returns a URL. A new `QuestionImageMCQ.vue` component handles the taker-side grid. The admin builder gets a type-aware option editor.

**Tech Stack:** Laravel 11, Inertia.js, Vue 3 (Composition API), Tailwind CSS, axios

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `app/Http/Controllers/Admin/AdminSurveyImageController.php` | Create | Accept image upload, store to public disk, return URL |
| `routes/web.php` | Modify | Add `POST /admin/survey-images` route |
| `app/Http/Controllers/Admin/AdminSurveyController.php` | Modify | Add `image_mcq` to question type enum + options validation |
| `resources/js/components/Survey/QuestionImageMCQ.vue` | Create | 2-col image grid component for survey takers |
| `resources/js/pages/SurveyActive.vue` | Modify | Import component, add render branch, add to auto-advance check |
| `resources/js/pages/admin/AdminSurveys.vue` | Modify | Image option builder, upload function, type-change handler |

---

### Task 1: Upload Controller

**Files:**
- Create: `app/Http/Controllers/Admin/AdminSurveyImageController.php`

- [ ] **Step 1: Create the controller**

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class AdminSurveyImageController extends Controller
{
    public function upload(Request $request)
    {
        $request->validate([
            'image' => ['required', 'image', 'max:2048'],
        ]);

        $path = $request->file('image')->store('survey-images', 'public');

        return response()->json([
            'url' => '/storage/' . $path,
        ]);
    }
}
```

- [ ] **Step 2: Verify the public storage symlink exists**

```bash
php artisan storage:link
```

Expected: `The [public/storage] link has been connected to [storage/app/public].` (or "already exists" — both are fine)

- [ ] **Step 3: Commit**

```bash
git add app/Http/Controllers/Admin/AdminSurveyImageController.php
git commit -m "feat: add AdminSurveyImageController for survey image uploads"
```

---

### Task 2: Upload Route

**Files:**
- Modify: `routes/web.php`

- [ ] **Step 1: Add the import and route**

At the top of `routes/web.php`, add the import alongside the other Admin imports:

```php
use App\Http\Controllers\Admin\AdminSurveyImageController;
```

Inside the `Route::prefix('admin')->name('admin.')->middleware('admin')` group, after the existing survey routes (around line 113), add:

```php
Route::post('/survey-images', [AdminSurveyImageController::class, 'upload'])->name('surveys.images.upload');
```

- [ ] **Step 2: Verify the route is registered**

```bash
php artisan route:list --name=admin.surveys.images.upload
```

Expected output includes a row with `POST | admin/survey-images`.

- [ ] **Step 3: Commit**

```bash
git add routes/web.php
git commit -m "feat: add POST /admin/survey-images upload route"
```

---

### Task 3: Backend Validation

**Files:**
- Modify: `app/Http/Controllers/Admin/AdminSurveyController.php`

Both the `store()` and `update()` methods have identical validation rules. Apply the same change to both.

- [ ] **Step 1: Add `image_mcq` to the question type enum**

Find this line in **both** `store()` and `update()`:
```php
'questions.*.type' => ['required', 'in:mcq,scale,checkbox,text'],
```

Replace with:
```php
'questions.*.type' => ['required', 'in:mcq,scale,checkbox,text,image_mcq'],
```

- [ ] **Step 2: Add `image_mcq` to the options required_if rule**

Find this line in **both** `store()` and `update()`:
```php
'questions.*.options' => ['required_if:questions.*.type,mcq', 'required_if:questions.*.type,checkbox', 'nullable', 'array'],
```

Replace with:
```php
'questions.*.options' => ['required_if:questions.*.type,mcq', 'required_if:questions.*.type,checkbox', 'required_if:questions.*.type,image_mcq', 'nullable', 'array'],
```

- [ ] **Step 3: Commit**

```bash
git add app/Http/Controllers/Admin/AdminSurveyController.php
git commit -m "feat: allow image_mcq question type in survey validation"
```

---

### Task 4: QuestionImageMCQ Component

**Files:**
- Create: `resources/js/components/Survey/QuestionImageMCQ.vue`

- [ ] **Step 1: Create the component**

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { useHaptics } from '@/composables/useHaptics';

const props = defineProps<{
    question: {
        id: string | number;
        text: string;
        hint?: string;
        options?: { label: string; image_url: string }[];
    };
}>();

const emit = defineEmits(['answer']);
const selected = ref<string | null>(null);
const { lightClick } = useHaptics();

function select(label: string) {
    lightClick();
    selected.value = label;
    emit('answer', { questionId: props.question.id, answer: label });
}
</script>

<template>
    <div class="grid grid-cols-2 gap-3">
        <button
            v-for="opt in question.options"
            :key="opt.label"
            type="button"
            @click="select(opt.label)"
            :class="[
                'rounded-2xl overflow-hidden border-2 transition-all duration-200 text-left bg-card',
                selected === opt.label
                    ? 'border-indigo-500 shadow-[0_0_0_3px_rgba(99,102,241,0.15)]'
                    : 'border-border hover:border-indigo-300',
                selected !== null && selected !== opt.label ? 'opacity-50' : '',
            ]"
        >
            <div class="relative">
                <img
                    :src="opt.image_url"
                    :alt="opt.label"
                    class="w-full h-28 object-cover"
                />
                <div
                    v-if="selected === opt.label"
                    class="absolute top-2 right-2 w-6 h-6 rounded-full bg-indigo-500 flex items-center justify-center shadow-lg"
                >
                    <svg class="w-3.5 h-3.5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                    </svg>
                </div>
            </div>
            <div class="px-3 py-2">
                <span
                    :class="[
                        'text-xs font-bold',
                        selected === opt.label ? 'text-indigo-600' : 'text-foreground',
                    ]"
                >
                    {{ opt.label }}
                </span>
            </div>
        </button>
    </div>
</template>
```

- [ ] **Step 2: Commit**

```bash
git add resources/js/components/Survey/QuestionImageMCQ.vue
git commit -m "feat: add QuestionImageMCQ taker-side component"
```

---

### Task 5: Wire Image MCQ into SurveyActive

**Files:**
- Modify: `resources/js/pages/SurveyActive.vue`

- [ ] **Step 1: Add the import**

At the top of the `<script setup>` block, alongside the other component imports (around line 7–10):

```typescript
import QuestionImageMCQ from '@/components/Survey/QuestionImageMCQ.vue';
```

- [ ] **Step 2: Add `image_mcq` to the auto-advance check**

Find this line (around line 104):
```typescript
if (question.type === 'mcq' || question.type === 'MCQ' || question.type === 'scale') {
```

Replace with:
```typescript
if (question.type === 'mcq' || question.type === 'MCQ' || question.type === 'scale' || question.type === 'image_mcq') {
```

- [ ] **Step 3: Add the render branch in the template**

Find the `QuestionText` block in the template (around line 306–311):
```vue
<QuestionText
    v-else-if="currentQuestion.type === 'text'"
    :question="currentQuestion"
    class="story-mode-text"
    @answer="(payload) => handleAnswer(currentQuestion, payload)"
/>
```

Add the new branch directly after it:
```vue
<QuestionImageMCQ
    v-else-if="currentQuestion.type === 'image_mcq'"
    :question="currentQuestion"
    class="story-mode-image-mcq"
    @answer="(payload) => handleAnswer(currentQuestion, payload)"
/>
```

- [ ] **Step 4: Commit**

```bash
git add resources/js/pages/SurveyActive.vue
git commit -m "feat: render image_mcq questions in survey taker view"
```

---

### Task 6: Admin Builder — Script

**Files:**
- Modify: `resources/js/pages/admin/AdminSurveys.vue` (script section)

- [ ] **Step 1: Add helper functions after `removeOption`**

Find the existing `removeOption` function (around line 275–277) and add these three functions immediately after it:

```typescript
const onQuestionTypeChange = (question: any) => {
    if (question.type === 'image_mcq') {
        question.options = [
            { label: 'Option 1', image_url: '' },
            { label: 'Option 2', image_url: '' },
        ];
    } else if (
        (question.type === 'mcq' || question.type === 'checkbox') &&
        question.options?.length &&
        typeof question.options[0] === 'object' &&
        'image_url' in question.options[0]
    ) {
        question.options = ['Option 1', 'Option 2'];
    }
};

const addImageOption = (qIndex: number) => {
    const count = form.questions[qIndex].options.length + 1;
    form.questions[qIndex].options.push({ label: `Option ${count}`, image_url: '' });
};

const removeImageOption = (qIndex: number, oIndex: number) => {
    if (form.questions[qIndex].options.length > 2) {
        form.questions[qIndex].options.splice(oIndex, 1);
    }
};

const uploadOptionImage = async (qIndex: number, oIndex: number) => {
    const input = document.createElement('input');
    input.type = 'file';
    input.accept = 'image/*';
    input.onchange = async (e) => {
        const file = (e.target as HTMLInputElement).files?.[0];
        if (!file) return;
        const formData = new FormData();
        formData.append('image', file);
        try {
            const { data } = await axios.post(
                route('admin.surveys.images.upload', [], false, page.props.ziggy),
                formData,
                { headers: { 'Content-Type': 'multipart/form-data' } }
            );
            form.questions[qIndex].options[oIndex].image_url = data.url;
        } catch (err) {
            console.error('Image upload failed', err);
        }
    };
    input.click();
};
```

- [ ] **Step 2: Commit script changes**

```bash
git add resources/js/pages/admin/AdminSurveys.vue
git commit -m "feat: add image MCQ helper functions to survey builder"
```

---

### Task 7: Admin Builder — Template

**Files:**
- Modify: `resources/js/pages/admin/AdminSurveys.vue` (template section)

- [ ] **Step 1: Add "Image Choice" to the question type select and wire the change handler**

Find the question type `<select>` (around line 797):
```html
<select v-model="question.type" class="w-full rounded-xl border-border border p-3.5 bg-muted/30 text-foreground font-black text-sm focus:ring-4 focus:ring-purple-500/10 focus:border-purple-500 transition-all">
    <option value="mcq">Multiple Choice</option>
    <option value="scale">Linear Scale</option>
    <option value="checkbox">Checkboxes</option>
    <option value="text">Text Entry</option>
</select>
```

Replace with:
```html
<select v-model="question.type" @change="onQuestionTypeChange(question)" class="w-full rounded-xl border-border border p-3.5 bg-muted/30 text-foreground font-black text-sm focus:ring-4 focus:ring-purple-500/10 focus:border-purple-500 transition-all">
    <option value="mcq">Multiple Choice</option>
    <option value="scale">Linear Scale</option>
    <option value="checkbox">Checkboxes</option>
    <option value="text">Text Entry</option>
    <option value="image_mcq">Image Choice</option>
</select>
```

- [ ] **Step 2: Add the image option builder block**

Find the MCQ/Checkbox options block (around line 806–828):
```html
<!-- MCQ/Checkbox Options -->
<div v-if="question.type === 'mcq' || question.type === 'checkbox'" class="space-y-3 pl-4 border-l-4 border-purple-500/20">
```

Add the following block **directly after** the closing `</div>` of that block (after line ~828):

```html
<!-- Image MCQ Options -->
<div v-if="question.type === 'image_mcq'" class="space-y-3 pl-4 border-l-4 border-purple-500/20">
    <div class="flex items-center justify-between mb-4">
        <label class="text-[10px] font-black uppercase tracking-widest text-foreground">Image Options</label>
    </div>
    <div
        v-for="(option, oIndex) in question.options"
        :key="oIndex"
        class="flex items-center gap-3 animate-in fade-in slide-in-from-left-2 duration-200"
    >
        <button
            type="button"
            @click="uploadOptionImage(qIndex, oIndex)"
            class="relative w-14 h-12 rounded-xl overflow-hidden border-2 border-dashed border-purple-300 flex-shrink-0 hover:border-purple-500 transition-all bg-purple-50 dark:bg-purple-900/20"
            title="Click to upload image"
        >
            <img
                v-if="option.image_url"
                :src="option.image_url"
                :alt="option.label"
                class="w-full h-full object-cover"
            />
            <div v-else class="w-full h-full flex items-center justify-center">
                <Plus class="w-5 h-5 text-purple-400" />
            </div>
        </button>
        <div class="flex-1 relative">
            <div class="absolute left-4 top-1/2 -translate-y-1/2 text-[9px] font-black text-muted-foreground bg-muted w-5 h-5 flex items-center justify-center rounded-lg">
                {{ oIndex + 1 }}
            </div>
            <input
                v-model="option.label"
                type="text"
                placeholder="Option label"
                class="w-full rounded-xl border-border border py-3 pl-11 pr-4 bg-background text-foreground text-sm font-bold focus:border-purple-500 focus:ring-0 transition-all"
                required
            />
        </div>
        <button
            type="button"
            @click="removeImageOption(qIndex, oIndex)"
            :disabled="question.options.length <= 2"
            class="p-2 text-muted-foreground hover:text-red-500 transition-colors disabled:opacity-30 disabled:cursor-not-allowed"
        >
            <X class="w-4 h-4" />
        </button>
    </div>
    <button
        type="button"
        @click="addImageOption(qIndex)"
        class="inline-flex items-center gap-2 px-4 py-2 text-[10px] font-black uppercase tracking-widest text-purple-600 hover:bg-purple-50 rounded-xl transition-all mt-2"
    >
        <Plus class="w-3.5 h-3.5" />
        Add Image Option
    </button>
</div>
```

- [ ] **Step 3: Commit template changes**

```bash
git add resources/js/pages/admin/AdminSurveys.vue
git commit -m "feat: add image MCQ option builder to admin survey form"
```

---

### Task 8: Build & Smoke Test

- [ ] **Step 1: Run the frontend build to check for TypeScript/compilation errors**

```bash
npm run build
```

Expected: exits with code 0, no errors.

- [ ] **Step 2: Verify `storage:link` is in place**

```bash
ls public/storage
```

Expected: directory exists (symlink to `storage/app/public`).

- [ ] **Step 3: Manual smoke test — builder**

1. Go to `/admin/surveys` → Create New Survey
2. Add a question, change type to "Image Choice"
3. Confirm options reinitialize as image option rows (thumbnail + label)
4. Click a thumbnail — file picker opens — pick a JPG/PNG
5. Confirm thumbnail shows the uploaded image
6. Add a second option, type a label
7. Save the survey

- [ ] **Step 4: Manual smoke test — taker**

1. Open the survey as a regular user
2. Navigate to the image_mcq question
3. Confirm 2-column grid renders with the uploaded images
4. Tap one — confirm indigo border + checkmark appear, other tiles dim
5. Confirm survey auto-advances after ~400ms

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat: image choice question type — end to end"
```
